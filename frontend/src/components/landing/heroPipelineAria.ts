/**
 * Single source of truth for the pipeline hero `role="img"` accessible name.
 * Used by `HeroPipelineVisual` and `AnimatedPipelineHero` so screen reader
 * output stays byte-identical across static and animated paths (Rule 27 I-8).
 */
export const HERO_PIPELINE_IMG_ARIA_LABEL =
  'Example ResearchOne flow grouped into four phases: Plan, Retrieve and Read, Reason and Verify, Write and Cite';
