/**
 * LiveRunPage - Real-time research progress view
 */

import { LiveRunPanel } from '@/components/r1-dashboard/LiveRunPanel';

export default function LiveRunPage() {
  return <LiveRunPanel />;
}
