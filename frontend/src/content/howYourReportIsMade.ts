/** Canonical Tier A copy — reuse verbatim on marketing surfaces and plain intro sections. */

export const HOW_YOUR_REPORT_IS_MADE_HEADING = 'How your report is made';

export const HOW_YOUR_REPORT_IS_MADE_STEPS = [
  'We turn your question into a research plan and show it to you to approve before anything runs—including the detected intent and epistemic posture.',
  'We find and rank relevant sources, then read them.',
  'We draft findings with a citation for every claim.',
  'When the intent warrants it, a skeptic step argues against the draft to catch weak or unsupported claims; investigation paths also seek supporting evidence for contested claims.',
  'We keep genuine disagreements between sources visible instead of hiding them.',
  'We verify every citation before finalizing, and you can export the report.',
] as const;

/** Approved marketing one-liner for the skeptic step. */
export const SKEPTIC_MARKETING_ONE_LINER =
  'Research that argues against itself before it concludes.';

/** Celebrates outlier bridging without forcing adversarial mode on every user. */
export const OUTLIER_BRIDGING_ONE_LINER =
  'When investigation is the goal, we hunt for slivers of truth and bridges to outlier evidence—without forcing every question into adversarial mode.';
