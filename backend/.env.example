# ResearchOne Backend environment template pointer
#
# Use one of the explicit templates below instead of this file:
# - Production (Emma runtime VM):  cp .env.production.example .env
# - Development (local machine):   cp .env.development.example .env
#
# This file is intentionally comment-only to avoid mixing production and development defaults.
#
# Research One 2 (Hugging Face uncensored models): set HF_TOKEN in `.env.development.example` / `.env.production.example` when using engine v2.
HF_TOKEN=
