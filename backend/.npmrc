# devDependencies are required to build, typecheck, lint and test this package.
# Without this, a machine or CI runner with NODE_ENV=production makes `npm ci`
# silently skip them, and the failure surfaces later as a confusing
# "tsc is not recognized" or an ERR_INVALID_ARG_TYPE from a lifecycle script.
include=dev
